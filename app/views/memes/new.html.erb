<h1>Upload a new Meme!</h1>

<%= form_for @meme do |m| %>
    <%= m.text_field :url, placeholder: "Image URL" %>
    <br>
    <%= m.text_field :caption, placeholder: "Caption..." %>
    <%= m.submit %>
<% end %>